#include <stdio.h>
#include<stdlib.h>
#include "Signs_Int.h"
#include"Signs_Double.h"
int main() {
	OutInt(AddInt(10, 20));
	OutDouble(AddDouble(0.1, 0.2));
	IntPolym* Arr = malloc(sizeof(IntPolym));
}